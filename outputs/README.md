# Outputs

This directory is used during experimentation to store generated artifacts such as
trained model checkpoints, cached predictions, evaluation metrics, plots, and
explainability visualizations.

These artifacts are not tracked in the repository to keep it lightweight and to
avoid committing large or files. All quantitative results and
qualitative examples are reported directly in the thesis.
